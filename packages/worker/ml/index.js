/* Functions to call index.py & return relevant data. This is the link from python to the rest of the codebase
  * Make sure secrets are passed to the child process
  * Try to limit saves to disk
  * Make sure pip install is run before any python commands... probably in the worker start script
*/
